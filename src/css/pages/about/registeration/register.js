import { makeStyles } from '@material-ui/core/styles'

const RegistrationPageStyles = makeStyles(theme => ({
  MainBackgroundRegister: {
    background: 'red',
  },
}))

export default RegistrationPageStyles
